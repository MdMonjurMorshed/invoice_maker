import 'package:flutter/material.dart';

class AppColor {
  //Global textfield style starts
  final appBarColor = Colors.black;

  final internetSnackbarColor = Colors.black;

  // bottom sheet print button border color
  final printButtonBorder = Colors.white;

  final focusBorderColor = Colors.purple[400];
  final prefixIconColor = Colors.purple[400];
  final cussorColor = Colors.purple[400];

  // radio active color
  final radioActiveColor = Colors.black;

  // text field cursor color
  final textFieldCursorColor = Colors.black;

  //Global textfield style ends
  // Login page theme starts
  final signInBtn = Colors.purple[400];
  final loginSnackError = Colors.red;
  // Login page theme ends

  // signup page theme starts
  final signupBtn = Colors.purple[400];
  final signupFocusBorderColor = Colors.purple[400];
  final signupPrefixIconColor = Colors.purple[400];
  //signup page theme ends

  // bid product form page starts

  // bid product from page ends

  // auction gellary starts
  final productTileBorderColor = Colors.purple[400];
  final productTileColor = Colors.white;
  final productTileShadowColor = Colors.purple[200];

  // auction gellary ends

  // auction page starts
  final tableBorderColor = Colors.purple[400];
  // auction page ends
  AppColor();
}
